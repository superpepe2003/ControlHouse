@model IEnumerable<ControlHouse.Models.SubCategoriaModel>

@{
    ViewBag.Title = "Sub Categoria";
}

<h2 style="text-align:center">Sub Categorias</h2>

<div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header" style="background-color:orange">
                <button type="button" class="close btn btn-info" data-dismiss="modal">&times;</button>
                <h3 class="modal-title" style="text-align:center">@ViewBag.Title</h3>
            </div>
            <div class="modal-body">
                <div id="modal-content">
                    Cargando.....
                </div>
            </div>
        </div>

    </div>
</div>

<p>
    <a class="btn btn-success" href="#myModal" id="btnNuevo" data-toggle="modal">Crear Nuevo</a>
    @*@if(ViewBag.DeCategoria)
    {
        string Idd = ViewBag.IdCategoria.ToString();
        @Html.ActionLink("Create Nueva", "Create", null, new { Id = Idd }, new { @class = "btn btn-success" });
    }
    else
    {
        @Html.ActionLink("Crear Nueva", "Create", null, new { @class = "btn btn-success" });
    }*@
</p>
<table class="table table-condensed">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categoria.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>        
        <td>        
            @Html.DisplayFor(modelItem => item.Categoria.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DropDown((List<DropDownLists>)ViewBag.Menu, "Acciones", item.Id, "myModal")
            @*@Html.DropDown((List<DropDownLists>)ViewBag.Menu, "Acciones", item.Id)*@
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
        </td>
    </tr>
}

</table>
@section scripts {
    @Scripts.Render("~/bundles/SubCategoriasjs")
}